# M9 — Manufacturing hardening

**Status:** In progress  
**Validates:** Definition of done #1 and #6 — documented install from scratch; second identical NUC built same way.

## Goal

Production-ready install documentation, release tagging, second-unit validation, and final v1 checklist.

## Prerequisites

- [M1](M1.md)–[M8](M8.md) complete
- Access to a second Asus NUC (or wipe/reinstall first unit for repeat test)

## Implementation steps

### 1. Manufacturing doc

Finalize [`docs/install-manufacturing.md`](../install-manufacturing.md):

- Ubuntu 26.04 install checklist (24.04 also supported)
- Exact commands in order
- Smoke test checklist via [`scripts/dev/smoke-m9.sh`](../../scripts/dev/smoke-m9.sh)
- SSH hardening posture for shipped units
- Version pin: `git clone --branch v1.0.0` (pending release tag)

**Done:** Expanded §3 with automated + manual checklists; aligned Ubuntu version notes.

### 2. Release process

- Bump [`VERSION`](../../VERSION) to `1.0.0`
- Git tag `v1.0.0` aligned with VERSION
- Document tag workflow in README

### 3. Second-unit test

Repeat full install on second NUC without dev shortcuts:

1. Manual Ubuntu install
2. Clone tagged release
3. Run `00` → `03` install scripts
4. Complete setup wizard
5. Create users, test SMB, snapshot restore, helper
6. Reboot test
7. Record any script fixes needed for idempotency

### 4. v1 definition of done checklist

From [`PROJECT_CONTEXT.md`](../../PROJECT_CONTEXT.md):

- [x] Dev NUC (`nuc1`) — automated smoke via `smoke-m9.sh` (Jul 2026)
- [ ] Fresh NUC installed from documented scripts only
- [x] Owner setup wizard + user/folder management via dashboard (UI polish Jul 2026)
- [x] Helper maps drives; Explorer read/write (verified on dev PC)
- [x] Nightly snapshots + dashboard restore (API smoke OK)
- [ ] Reboot survival without manual steps (manual check pending)
- [ ] Second unit identical install

### 5. README final pass

- Update status section to v1 complete (after tag + second unit)

### 6. UI polish (owner + employee UX)

**Done (Jul 2026):**

- Dashboard design refresh ([`dashboard/src/index.css`](../dashboard/src/index.css))
- 3-step setup wizard ([`SetupPage.tsx`](../../dashboard/src/pages/SetupPage.tsx))
- Employee helper guide on System page ([`SystemPage.tsx`](../../dashboard/src/pages/SystemPage.tsx))
- WPF login window restyle ([`LoginWindow.xaml`](../../helper/FrogsWork.Helper/UI/LoginWindow.xaml))
- Shared components: `StepGuide`, `GuideCard`, `PageIntro`, `CopyButton`

### 7. Optional hardening

- Remove dev-only SSH password auth if still enabled
- Log rotation for API logs
- `frogswork-firstboot.service` real checks or disable after setup

## Verification

- [x] `smoke-m9.sh` passes on `nuc1`
- [x] 37 backend pytest tests pass
- [x] Dashboard + helper build and deploy to `nuc1`
- [ ] Second NUC passes full checklist
- [ ] Install doc followed by someone **not** the original developer (if possible)

## Suggested commits

1. `M9: add smoke-m9 script and expand manufacturing checklist`
2. `M9: refresh dashboard design system and setup wizard`
3. `M9: add employee helper guide and polish WPF login`

## Project complete

All M0–M9 milestones done. Future work (out of v1 scope): HTTPS, Mac helper, remote VPN, code-signed helper.
