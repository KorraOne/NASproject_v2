# M0 — Scaffold

**Status:** Complete  
**Validates:** Repo structure agreed; local builds and one pytest pass on laptop.

## Goal

Create the greenfield repository layout, deploy templates, and package stubs with **no business logic**. Rework product branding in `PROJECT_CONTEXT.md` and add development documentation.

## Prerequisites

- Windows laptop with git, Python 3.12+, Node.js 20+, .NET 8 SDK
- No NUC required

## Implementation steps

### 1. Product and process docs

- [x] Rework [`PROJECT_CONTEXT.md`](../../PROJECT_CONTEXT.md) — FrogsWork / KorraOne branding, user model, defaults
- [x] Add [`docs/development-behaviour.md`](../development-behaviour.md)
- [x] Add doc stubs: `architecture.md`, `install-manufacturing.md`, `api.md`
- [x] Expand [`README.md`](../../README.md) with layout and dev prerequisites

### 2. Repo hygiene

- [x] Add [`.gitignore`](../../.gitignore) — Python, Node, .NET, secrets, `.cursor/`
- [x] Add [`VERSION`](../../VERSION) — `0.0.0-dev`

### 3. Backend skeleton

- [x] [`backend/pyproject.toml`](../../backend/pyproject.toml) — FastAPI, uvicorn, pytest dev deps
- [x] [`backend/frogswork_api/main.py`](../../backend/frogswork_api/main.py) — empty FastAPI app
- [x] Package dirs: `auth/`, `users/`, `folders/`, `snapshots/`, `system/`, `integrations/`
- [x] Integration stubs: `samba.py`, `linux_users.py`, `btrfs.py`
- [x] [`backend/tests/test_main.py`](../../backend/tests/test_main.py) — minimal app title test

### 4. Dashboard skeleton

- [x] React + Vite + TypeScript in [`dashboard/`](../../dashboard/)
- [x] Placeholder `App.tsx` shell
- [x] Empty `src/pages/` and `src/api/` dirs

### 5. Helper skeleton

- [x] [`helper/FrogsWork.Helper/`](../../helper/FrogsWork.Helper/) — .NET 8 WPF project
- [x] `Program.cs` throws `NotImplementedException` until M8
- [x] Empty `Discovery/`, `DriveMapper/` dirs

### 6. Deploy templates

- [x] [`deploy/nginx/frogswork.conf`](../../deploy/nginx/frogswork.conf)
- [x] [`deploy/avahi/frogswork.service`](../../deploy/avahi/frogswork.service)
- [x] [`deploy/systemd/`](../../deploy/systemd/) — api, snapshot, timer, firstboot units
- [x] [`scripts/samba/templates/smb.conf`](../../scripts/samba/templates/smb.conf)

### 7. Script placeholders

- [x] [`scripts/install/00`–`03`](../../scripts/install/) — exit with message until M1
- [x] [`scripts/btrfs/`](../../scripts/btrfs/) — placeholders until M6
- [x] [`scripts/dev/sync.sh`](../../scripts/dev/sync.sh) — placeholder until M1

## Verification (laptop)

```powershell
# Backend
cd backend
python -m venv .venv
.\.venv\Scripts\pip install -e ".[dev]"
.\.venv\Scripts\pytest

# Dashboard
cd dashboard
npm install
npm run build

# Helper
cd helper\FrogsWork.Helper
dotnet build
```

Expected: pytest 1 passed; dashboard `dist/` created; helper builds with 0 errors.

## Commits made

| Commit | Summary |
|--------|---------|
| `c863b0f` | Development behaviour plan + README links |
| `a91345c` | PROJECT_CONTEXT FrogsWork branding rework |
| `9268927` | `.gitignore`, VERSION, backend skeleton |
| `f4423c5` | Dashboard, helper, deploy templates, script stubs |
| `2de756a` | Doc stubs + README expansion |

## Handoff to M1

- Install scripts exist but **must not be run** until M1 implements them.
- NUC needs **Ubuntu Server 24.04** with SSH before starting M1.
- Next: [`M1.md`](M1.md)

## Dev steps index

All milestone guides: [`docs/dev-steps/README.md`](dev-steps/README.md)
