/** Placeholder shell — pages land in M5. */

export function App() {
  return (
    <main style={{ fontFamily: "system-ui, sans-serif", padding: "2rem" }}>
      <h1>FrogsWork File Storage</h1>
      <p>Dashboard scaffold (M0). Setup wizard and admin UI arrive in M5.</p>
    </main>
  );
}
